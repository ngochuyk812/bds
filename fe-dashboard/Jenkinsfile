@Library('devops@develop') _

pipeline {
    agent any
    environment {
        NAME = "fe-dashboard"
    }
    stages {
        stage('Build & Deploy') {
            steps {
                script {
                    buildHelper(env.NAME, "react")
                }
            }
        }
    }
}
